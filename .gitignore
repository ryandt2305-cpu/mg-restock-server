# dependencies
node_modules/

# local examples
restock examples/

# local cache
*.log
.DS_Store

# local env
.env

# supabase cli temp files (contain project ref, pooler url)
supabase/.temp/

# superpowers session artifacts (local only, never push)
docs/superpowers/

# keep data + scripts + workflow + package files tracked

